interface metaDataProps {
  baseUrl: string;
  themeColor: string;
  author: string;
  authorDescription: string;
  authorLink: string;
}

const metadata: metaDataProps = {
  baseUrl: "https://insportzclub.com",
  themeColor: "#0f0f0f;",
  author: "Insportz",
  authorDescription: "",
  authorLink: "https://insportzclub.com",
};

export default metadata;
